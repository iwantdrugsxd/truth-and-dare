class ApiConfig {
  // Reveal Me's backend — a separate Vercel project (Node/Express +
  // Postgres) from the one hosting this Flutter web app.
  static const String baseUrl = 'https://partizo-backend.vercel.app/api';
}
